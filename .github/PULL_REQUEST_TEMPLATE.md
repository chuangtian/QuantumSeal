## What this changes

## Why

## Checklist

- [ ] `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test` pass
- [ ] Viewer typecheck and tests pass (if `viewer/` was touched)
- [ ] Docs updated (`docs/RULES.md`, README) where behavior changed
